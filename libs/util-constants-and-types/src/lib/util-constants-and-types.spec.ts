import { utilConstantsAndTypes } from './util-constants-and-types';

describe('utilConstantsAndTypes', () => {
  it('should work', () => {
    expect(utilConstantsAndTypes()).toEqual('util-constants-and-types');
  });
});
