import { WithdrawalStatus } from '../types';

export const mockWithdrawalStatus: WithdrawalStatus = {
  mode: 'errored',
  venue: 'polymarket',
  positions_total: 7,
  positions_sold: 5,
  positions_stuck: 2,
  fills: [],
  errors: [],
};
