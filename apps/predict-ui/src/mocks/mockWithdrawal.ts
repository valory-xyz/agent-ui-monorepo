import { WithdrawalStatus } from '../types';

export const mockWithdrawalStatus: WithdrawalStatus = {
  mode: 'armed',
  venue: 'polymarket',
  positions_total: 0,
  positions_sold: 0,
  positions_stuck: 0,
  fills: [],
  errors: [],
};
